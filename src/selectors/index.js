// import { createSelector } from 'reselect';

// export const getNoticesSelector = state => {
// 	return state.notices.items;
// };

// export const filteredNotices = createSelector(
// 	[getNoticesSelector],
// 	(notices, id) => {
// 		return notices.filter(notice => notice.directoryId === id);
// 	}
// );
